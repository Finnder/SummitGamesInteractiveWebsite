# Hello World

*August 28, 2026*

This is a placeholder post. Replace it (or keep it around as a reference for the formatting below).

## Writing a post

Posts are plain Markdown files in `content/blog/`. Everything standard works:

- Lists like this one
- **Bold**, *italic*, and `inline code`
- [Links](games.html) and images

> Blockquotes look like this.

```
Code blocks look like this.
```

## Publishing

Drop the `.md` file into `content/blog/`, add its filename on a line in `content/blog/posts.txt`, push. Done. The card on the blog page uses your `# Title`, the `*date line*` under it, and the first paragraph — all straight from this file.
